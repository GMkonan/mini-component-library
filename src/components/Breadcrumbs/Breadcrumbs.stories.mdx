import { Meta, Story } from '@storybook/addon-docs/blocks';
import {Breadcrumbs, Crumb} from './Breadcrumbs';

<Meta
  title="Breadcrumbs"
  component={Breadcrumbs}
  argTypes={{
    value: {
      control: {
        type: 'number',
        min: 0,
        max: 100,
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
  }}
/>

export const Template = (args) => (
  <Breadcrumbs {...args}>
    <Crumb href="/">teste</Crumb>
    <Crumb href="/">teste2</Crumb>
    <Crumb href="/">teste3</Crumb>
    <Crumb href="/">teste4</Crumb>
  </Breadcrumbs>
);

# Breadcrumbs

<Story name="Default" args={{ value: 50, size: 'medium' }}>
  {Template.bind({})}
</Story>